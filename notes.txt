points - очки (example: Q == 10)
scores - баллы: туз + валет == BJ - 10 баллов

def play():
    ...
    while not table.isFull():
        ...
        table.show()
        card = deck.nextCard()
        while True:
            number = ask_user()  # TODO
            try:
                table.putCardToCell(card, number)  # TODO
                break
            except NotAvailableCell() as e:
                print('ячейка недопустима')
            except CellIsBusy() as e:
                print('ячейка занята')


    lines = table.get_lines()
    result_score = 0
    for line in lines:
        score = scoreResolver.calculateScoreForLine(line)
        result_score += score

    print(result_score)
    return 0

class BSLine:
    def __init__(self, cards: Line[Card]):
        self.cards: List[Card] = cards
        self.threshold = 21

    def getPoints(self) -> int:
        ...

